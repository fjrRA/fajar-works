// src/types/site.ts

export type SiteContent = {
  siteName: string;
  siteCode: string;
  name: string;
  role: string;
  location: string;
  locationCode: string;
  tagline: string;
  description: string;
  availability: string;
  availabilityCode: string;
};